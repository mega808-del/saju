# 🔮 이성효의 명리학 - 최종 전체 작업 내역 및 기능 명세서 (work.md)

본 문서는 **이성효의 명리학 사주팔자 안드로이드 모바일 프로그램**의 전체 개발 내역, 기능 명세, 고도화 및 보정 사항을 집대성한 공식 작업 요약서입니다.

---

## 📌 1. 핵심 기능 및 모바일 터치 최적화 사양

### ① 모바일 핀치 줌(Pinch Zoom) & 제스처 컨트롤
- **두 손가락 핀치 줌**: 스마트폰 터치 화면에서 0.8배 ~ 3.5배까지 화면 자유 확대/축소 지원.
- **손가락 드래그(Pan)**: 화면 확대 상태에서 구석구석 자유 이동 가능.
- **상단 플로팅 모바일 툴바**:
  - `🔍+` / `🔍-` 줌 조작 버튼 및 줌 비율(%) 실시간 디스플레이.
  - `↺ 리셋` 더블 탭 및 원복 버튼.
  - `🔤+` / `🔤-` 폰트 개별 스케일링 기능.
  - ~~`📱 APK 설치`~~ **버튼 제거 완료**: APK 파일 부재로 작동하지 않아 사용자 요청으로 툴바에서 완전 삭제 (index.html/script.js/styles.css 관련 코드 제거, 다른 기능 영향 없음).

### ② 생년월일 직접 텍스트 입력 + 실제 탁상달력 이미지 버튼 듀얼 지원
- **직접 입력(Text Input)**: `19950515` 또는 `1995-05-15` 키보드 입력 시 자동 서식 지정(`YYYY-MM-DD`).
- **실제 탁상달력 이미지 버튼 (겹침 해결 완료)**: 입력창 오른쪽에 실제 탁상달력처럼 보이는 **Twemoji 찢어지는 달력(📆, 1F4C6, CC-BY 4.0)** 이미지 버튼 배치. HTML 인라인 SVG 내장 방식이라 **인터넷/APK 재빌드 여부와 무관하게 항상 표시**됨.
  - **달력 이미지 2배 확대(30px → 60px)** 적용, 버튼 크기 64px로 조정.
  - **달력 버튼 높이 입력칸 일치 보정**: 버튼과 숨김 날짜 입력을 고정 64px → `height:100%`(입력칸 높이 ≈51px) + `width:48px`로 변경하여, 달력이 아래 힌트 글자(※ 직접 타이핑...)를 가리지 않도록 세로 크기를 입력칸과 정확히 일치시킴. 이미지는 `height:80%` + `aspect-ratio:1/1`로 비례 축소, 입력창 `padding-right` 78px→62px, 호버 확대율 1.1→1.03으로 축소.
  - 버튼 배경을 **불투명 어두운 색**(`var(--bg-card)`)으로 변경하여 placeholder/입력 글자가 달력 이미지 뒤로 비치던 **겹침 현상 완전 제거** (입력창 자체는 수정하지 않음).
  - **네이티브 날짜 입력(‘년/월/일’ 흰색 박스) 완전 숨김 처리**: 기존에 그대로 노출되던 `<input type="date">`를 달력 버튼 위치에 투명하게 겹쳐 배치(`opacity:0`)하여 보이지 않게 하고, 클릭 시 `showPicker()` 네이티브 달력 팝업이 버튼 위치에 정확히 열리도록 보정.
  - 클릭 시 네이티브 달력 팝업을 즉시 오픈(`showPicker()`, 1인 운세 + 궁합 모드 양쪽 모두 적용).
  - 출처: Twitter Twemoji 1F4C6 (https://twemoji.twitter.com) · CC-BY 4.0, 원본 SVG는 `calendar.svg` 파일로 보존.

---

## 🔮 2. 5단계 사주 명리학 데이터 고도화 및 오류 완전 보정

1. **☯️ 음력 ↔ 양력 사주팔자(4주 8자) 100% 일치 보정**
   - `KoreanLunarCalendar` 알고리즘을 연결하여, 음력을 입력하더라도 동일 인물의 양력 생년월일로 자동 변환 후 4주(년/월/일/시)를 계산.
   - **오류 수정 완료**: 기존 CDN 참조 버전(`korean-lunar-calendar@1.0.12`)이 존재하지 않아 404로 라이브러리가 로드되지 않던 문제 수정 → 유효 버전 `@0.4.0`으로 교체, 음력 입력 시 양력과 100% 동일한 4주 8자 도출.
   - 변환 함수에 setter 반환값 검증 추가(범위 초과 시 `null` 반환 → 입력값 유지).
   - **결과**: 양력 `1990년 6월 7일` 또는 음력 `1990년 5월 15일` 입력 시 4주 8자(`庚午년 壬午월 戊辰일`) 및 운세 풀이가 100% 동일하게 도출됨.
   - 프로필에 `양력 1990년 6월 7일 (음력 1990년 5월 15일)` 동시 표시.

2. **24절기(節氣) 정밀 입절 처리**
   - 입춘, 경칩, 청명, 입하, 망종, 소서, 입추, 백로, 한로, 입동, 대설, 소한 12월절 정밀 시각 반영으로 월주(月柱) 판단의 정확도 확보.

3. **12운성 (十二運星) & 12신살 & 주요 특수신살**
   - 10천간 x 12지지 양순음역(陽順陰逆) 원리의 12운성(장생~절) 및 12신살(겁재천지년월망장반역육화) 포착.
   - 천을귀인, 문창귀인, 백호대살, 괴강살, 귀문관살, 현침살 등 명리학 핵심 길흉신살 자동 풀이.

4. **형·충·파·해·합 심화 매트릭스**
   - 육합(六合), 육충(六沖), 원진살(怨嗔殺), 삼합, 방합, 삼형살 정밀 탐지.

5. **신강/신약, 용신(用神), 희신(喜神), 격국(格局)**
   - 득령(월지 +40점), 득지(일지 +20점), 득세(타 기둥 각 +10점) 점수화 기반 일간 강약 판단 및 맞춤 용신/희신 오행 처방.

---

## 📊 3. 결과 섹션 엄격 정렬 순서 및 15년 세운 내비게이션

1. **프로필 & 사주팔자 카드** (년주, 월주, 일주, 시주)
2. **용신 및 격국 분석** (신강/신약 점수, 용신, 희신, 격국)
3. **대운 (大運)** (대운수, 순행/역행, 10년 단위 8대운 타임라인)
4. **오행 분석** (목화토금수 비율 그래프 및 보충 조언)
5. **십성 분석** (4주 십성 + 일간 십성 5개 영역 상세 특징 [성격, 재물, 애정, 직업, 주의사항])
6. **성격 및 특성** (운명 총평)
7. **12운성 · 12신살 · 형충파해 심화**
8. **궁합 분석** (커플 모드 시 원본 100% 동일 UI 출력)
9. **연도별 운세 (2026년~2040년 15년 세운)**
   - 상단 15년(2026년~2040년) 버튼 탭 배치.
   - 특정 연도 버튼(예: `2032년`) 클릭 시 해당 연도 카드가 **최상단(맨 위)으로 이동**되며 즉시 스크롤 및 광채 효과 적용.

---

## 💑 4. 원본 파일 궁합 기능 100% 동일 복원

- 첫 화면 `👤 개인 운세` vs `💑 궁합 보기` 토글 및 상대방 정보 입력 폼 100% 유지.
- 원본 궁합 계산식: **오행 조화(30%) + 지지 관계(30%) + 오행 상생(40%) = 100점 만점**.
- 원본 점수 배지(`85`), 등급(`최상`), 총평, 두 사람 VS 배너, 3개 프로그레스 바, 지지 합/충 내역 100% 완벽 보존.

---

## 🆕 v5.0~v5.1 동적 만세력 보완 내역 (입력값별 맞춤 사주팔자·풀이)

1. **일주(日柱) 오프셋 정밀 보정** — 1900-01-01=甲戌일 기준 `index=(diff+10)%60` (앵커: 1949-10-01=甲子일, 2000-01-01=戊午일, 2024-01-01=甲子일).
2. **월주(月柱) 래핑 보정** — 子/丑월(1월 초~입춘 전) 오프셋을 12지지 순환으로 처리 (2000-01-01=丙子월, 2024-01-01=甲子월).
3. **대운 기산 12월절(節) 기준 수정** — 中氣(춘분 등)를 절기로 오인하던 오류 제거. 예: 1992-03-10 남 4세→**9세**, 여 17세→**1세**.
4. **세운 연주 입춘 기준** — 연도별 운세 카드의 년주를 입춘(2/4 이후) 기준으로 계산 (2026년 → **丙午**).
5. **동적 맞춤 풀이 사전** — 일간 10종 성격(DAY_GAN_CHARACTER), 12운성·12신살 의미, 대운 십성별 해설, 세운 오행 관계 해설, 월간 십성 영향, 용신 동적 해설 (고정 문장 제거).
6. **음력 윤달(윤월) 지원** — 음력 선택 시 윤달 체크박스 표시(본인/상대방 각각), 계산·`(윤)` 표시 반영.
7. **입력 오류 방어** — 존재하지 않는 날짜(예: 2023-02-30) 차단, 음력 모드에서 변환 라이브러리 미로드 시 차단, 계산 오류 try/catch + 토스트, 공통 헬퍼 `computeSaju`로 검증 단일화.
8. **UX 보강** — 연도 탭 중복 리스너 방지(onclick 재할당), 저장 데이터 복원(성별/달력/시간/윤달/궁합), 오행 보충 조언 표시, 저장 후 재조회 결과 일치.
9. **JSON 응답 규격** — `buildFortuneJSON()` (사주 8자 / 오행 / 십성 / 용신 / 대운 / 12운성·신살 / 성격 / 15년 운세).
10. **자체 검증** — 엔진 23건(일주·월주·대운·세운·윤달 앵커 등) + UI 12→13건 전부 PASS, 콘솔 에러 0.

※ v5.1까지 절기 경계는 고정일 근사(입춘 2/4 등, ±1일 편차 가능)였으나, **v5.2에서 정밀 절기 공식으로 대체되어 해소**되었습니다.

---

## 🆕 v5.2 동적 만세력 고도화 내역 (QA 비교 리포트 개선점 반영)

1. **정밀 절기(24절기) 천문 근사 공식** — 황경 15° 간격 + 뉴턴 반복으로 연도별 실제 입절 시각(시·분) 계산.
   - `TERM_GUESS` 24절기 추정일 테이블(12절+12중기) 기반 수렴, KST 고정.
   - 검증 앵커(공인값 vs 공식): 2024 춘분 12:04(±2분), 하지 05:49(±2분), 동지 18:14(±7분), **2023 동지 12:27(±0분)**, **1992 청명 21:45(±0분)**, 2024 입춘 17:21(공인 17:27, ±6분 — 16:27은 베이징시각 혼용 자료 주의).
   - `calcYearPillar`·`getMonthJiIdx`·대운 기산 `findNextJeolgi` 모두 절기 시각 기준으로 통합.
2. **야자시/정자시 규칙** — 23:00~24:00 자시에 **야자시분일 기본 적용** (다음날 일주·시주로 재계산, 일주 변경 시 오행·십성·용신·대운·세운 자동 연동).
   - UI 토글(기본값: 야자시분일) 지원, `isYajasi` 플래그로 야자시 적용 여부 JSON에 표기.
3. **격국(格局) 판정** — 월지 본기(藏干) 기준 격국명 자동 산출 + `GYEOKGUK_DESC` 격국별 해설 (정인격·정재격·건록격·상관생재격 등).
4. **형충파해(刑沖破害) 심화** — `analyzeHyeongChungPaHae`로 형(三刑)·충(六沖)·파(六破)·해(六害) 전체 조합 탐지 + 해설.
5. **육친관계** — 십성→육친 매핑(`YUKCHIN_BY_SIPSEONG`: 정인=母, 편재=父, 정재=妻, 정관=夫, 식신=子女 등) + 기둥별 육친 해설.
6. **12신살 전체(12개)** — 기존 4개(기둥별)에 더해 `allSinsal`로 년지 기준 12지지 전체 살표 제공.
7. **연도별 운세 카테고리 확장** — 기존 총운·재물·애정·건강·직장·주의사항에 **학업·금전·인덕운** 3종 추가 (`getCategoryFortune` 십성 10종별 문장).
8. **`buildFortuneJSON` v5.2** — `gyeokguk`, `hyeongchung.details`, `yukchin.details`, `allSinsal`, `saju.isYajasi/jasiRule` 필드 추가.
9. **자체 검증** — `_selfcheck2.html` **PASS 43 / FAIL 0** (절기 공식 앵커 8종, 입춘 경계, 야자시, 격국, 형충파해, 육친, 12신살, 윤달, A/B 가변성, 중복 0건), 콘솔 에러 0.

---

## 🆕 v5.3 날짜·시간 자동 인식 + 입춘(立春) 기준 연도 산정 (2026-08-06)

1. **시스템 날짜·시간 자동 인식 & 기준일시 표시** — 결과 화면 최상단에 `운세 조회 기준일시: YYYY년 MM월 DD일 HH:mm · 명리학 기준 올해 N년 (간지)` 배너 추가 (`#baseDateTime`, `renderBaseDateTime()` in script.js).
2. **입춘 기준 명리학 연도 판정 헬퍼** — `saju-engine.js`에 `getMyeongriYear(now)` 추가(export): 실제 입춘 시각(황경 315°, v5.2 정밀 절기 공식) 이전이면 전년도로 판정. 예: 2027-01-15 접속 → 입춘(2027-02-04 경) 이전 → **2026년(丙午)부터 15년 배열 생성**.
3. **15년 배열 시작 연도 교체** — `script.js` displayResults와 `buildFortuneJSON`의 `new Date().getFullYear()`(양력 1/1 기준 오류) → `getMyeongriYear()`(입춘 기준)로 통일.
4. **기존 프로그램 영향 없음** — 함수 시그니처 변경 0건, 연도별 세운의 개별 천간/지지는 기존 입춘 기준 로직 그대로(추가 오류 소지 없음), 대운 '현재' 표시는 종전 로직 유지.
5. **검증** — `_v53_datetime_test.html` **PASS 15 / FAIL 0** (입춘 경계 5종, 15년 배열 2026~2040·병오~경신, 연속성, 9개 카테고리, JSON 일관성) + 브라우저 실측(기준일시 배너·15개 버튼·병오 카드 정상, JS 콘솔 에러 0).

---

## 🆕 v5.4 연도 카드 순환 정렬 + 리셋 버튼 폼 초기화 (2026-08-06)

1. **연도별 운세 카드 순환 정렬 수정** — `renderYearNav` 클릭 시 `insertBefore`(1장만 이동 → 2027 클릭 시 [2027, 2026, 2028…] 순서 오류)를 **전체 카드 순환 정렬**(클릭 연도 → 다음 해 순, 2040 다음 2026 순환)로 교체 (`cards.sort` + `appendChild` 이동, 재렌더링 없음).
2. **↺ 리셋 버튼에 폼 초기화 추가** — 기존 줌 리셋만 하던 버튼에 `resetFormInputs()` 연동: 이름/생년월일(본인·상대방)/시간/성별/달력/윤달/궁합 모드/자시 기준 전부 기본값 복원 + 결과 화면이면 입력 폼으로 자동 복귀 (localStorage 저장 데이터는 유지).
3. **검증** — 브라우저 실측: 2027 클릭 → `[2027, 2028, …, 2040, 2026]` 순서 확인, 리셋 클릭 → 입력값 초기화 + 폼 복귀 확인, JS 콘솔 에러 0.

---

## 🧪 QA 비교 분석 스크립트 (saju_data_comparator.py)

1. **목적**: 내 사주 프로그램 출력 데이터 vs 표준 만세력 참조 데이터를 자동 비교·분석해 차이점 리포트를 생성 (QA Automation / Data Analysis).
2. **3개 테스트 표본** (경계 조건 반영):
   - Case 1: 1995-05-20 14:30 남 (일반 양력)
   - Case 2: 2024-02-04 16:00 남 (입춘 경계일 — 정밀 절기 16:27 이전)
   - Case 3: 1988-06-01 23:30 남 (야자시·서머타임)
3. **비교 레이어**: L1 사주 명식(4주 8자 간·지 개별 + 대운 기산·방향, 총 10항목) / L2 사주 요소(오행 5 + 십성 4) / L3 풀이 텍스트(입력별 가변성 + 해석 섹션 풍부도).
4. **입력 데이터**: `data/my_caseN.json`(내 프로그램 buildFortuneJSON 추출) · `data/ref_caseN.json`(표준 만세력 참조). `--demo` 모드는 `_demo_data.py` 내장 샘플 사용.
5. **리포트**: 정확도 점수(L1·L2·종합), 차이점·오류 목록, 데이터 풍부도(섹션 커버리지·항목 합계·평균 길이), 개선 권고 5건을 마크다운으로 자동 생성.
6. **v5.1 검증 결과**: L1 명식 100/50/60% · L2 요소 100/56/22% · 가변성 82% · 풍부도 커버리지 38% (누락 10개) → **v5.2 개선 후 재검증**: L1/L2 전 케이스 **100%** · 가변성 **85%** · 커버리지 **94%** (누락 **1개**: 보완 아이템), 콘솔 에러 0 (PASS 9/0).
7. **v5.2 풍부도 반영**: 격국·형충파해·육친·12신살 전체·학업/금전/인덕운 등 신규 해설 섹션을 `MY_TEXT_TO_CANONICAL` 정규 매핑(17개 섹션)에 반영 — 재물/애정/건강/직업/학업/금전/인덕운은 연도별 운세 카테고리 텍스트(15년치) 기준으로 집계 (리포트에 주석 명시).
7. **실행**: `python saju_data_comparator.py [--demo] [--cases 1,2,3] [--out report.md]`

---

## 📁 5. 프로젝트 주요 파일

- `index.html` - 모바일 최적화 뷰포트 & 듀얼 생년월일 입력 및 실제 탁상달력 이미지 버튼 (인라인 SVG 내장)
- `styles.css` - 럭셔리 동양풍 다크 모드 & 노란색 강조 스타일 & 핀치 줌 CSS
- `saju-engine.js` - 음양력 정밀 변환 & 5단계 명리학 연산 통합 엔진
- `script.js` - UI 연동, 15년 세운 맨 위 이동 스크립트, 오류 검증 완료 스크립트
- `pinch-zoom.js` - 멀티터치 핀치 줌 & 제스처 엔진
- `calendar.svg` - 달력 버튼 이미지 원본 (Twemoji 1F4C6 탁상달력, CC-BY 4.0)
- `work.md` - 전체 작업 내역 요약서
